{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd website1 && npm ci && npm run build",
  "outputDirectory": "website1/out",
  "installCommand": "echo 'root install skipped — website1 installs its own deps'",
  "framework": null
}
